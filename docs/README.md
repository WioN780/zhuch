# docs/

Internal working documents (contracts, runbooks, blog drafts) are kept local
and excluded from the public repository — only this README is tracked.

If you are reading this on GitHub: architecture and usage documentation lives
next to the code it describes (`backend/models/README.md`, `training/README.md`,
`analytics/README.md`, `infra/*/README.md`, `infra/k8s.md`).
